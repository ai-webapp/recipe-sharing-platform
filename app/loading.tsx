import PageSkeleton from "@/components/skeletons/PageSkeleton";

/**
 * Stato di caricamento globale: mostrato durante la navigazione tra le pagine.
 */
export default function Loading() {
  return <PageSkeleton />;
}
